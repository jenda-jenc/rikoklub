<!DOCTYPE html>
<html lang="cs">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title | default('Riko Klub') }}</title>
    <meta
      name="description"
      content="Riko Klub is a community-driven creative space hosting events, workshops, and art experiences."
    />
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header class="site-header">
      <div class="container">
        <a class="logo" href="/">Riko Klub</a>
        {% set navItems = navigation | default([]) %}
        <nav class="site-nav">
          {% for item in navItems %}
            <a href="{{ item.href }}">{{ item.label }}</a>
          {% endfor %}
        </nav>
      </div>
    </header>
    <main>
      {{ content | safe }}
    </main>
    <footer class="site-footer">
      <div class="container">
        <p>&copy; {% currentYear %} Riko Klub. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
